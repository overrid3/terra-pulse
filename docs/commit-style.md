# Commit style

Conventional commits. One concern per commit.

Examples:

```
feat(web): i18n EN + IT with IT default
fix(backend): branch SQL when nearest-mechanic radius is NULL
refactor(orders): extract ServiceOrderStateMachine guards
```
