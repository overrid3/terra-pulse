# Copy to .env.local and fill in real values.
# Get the publishable key from Clerk Dashboard → API Keys.
VITE_CLERK_PUBLISHABLE_KEY=pk_test_replace_with_your_key

# Local dev overrides (defaults work if backend runs on :8080)
# VITE_API_BASE=http://localhost:8080/api
# VITE_WS_BASE=ws://localhost:8080/ws
